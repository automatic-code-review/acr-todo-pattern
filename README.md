# acr-todo-pattern

Arquivo config.json

```json
{
    "message": "${FILE_PATH} - ${LINE_NUMBER} - ${TODO_CONTENT}",
    "regexId": [
        "#ACR-[0-9]*"
    ],
    "extensions": [
        ".py"
    ]
}
```
